import React from "react";
import { CircularProgress } from "@material-ui/core";

const Spinner: React.FC = () => {
    return <CircularProgress className="respSpinn" size={54}
    thickness={2}/>
}

export default Spinner


